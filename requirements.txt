streamlit==1.31.0  # Update to a newer version compatible with Python 3.12
numpy>=1.26.0  # Correct, this version supports Python 3.12
pandas>=2.0.3,<2.1.0  # Specify a compatible version 
matplotlib>=3.7.2,<3.8.0  # Update matplotlib
pillow>=10.0.0  # Add explicit pillow version that has wheels for Python 3.12
